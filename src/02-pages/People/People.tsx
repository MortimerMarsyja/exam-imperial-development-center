import ComposedTitle from "@components/ComposedTitle";
import { FC } from "react";

const People: FC = () => {
  return (
    <div>
      <ComposedTitle
        title="People"
        subtitle="We track them all, from the smallest to the largest"
      />
    </div>
  );
};

export default People;
